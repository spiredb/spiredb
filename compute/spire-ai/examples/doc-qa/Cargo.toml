[package]
name = "spire-ai-example-doc-qa"
version = "0.1.0"
edition = "2024"
publish = false
description = "Document Q&A system with RAG pipeline"

[workspace]

[dependencies]
spire-ai = { path = "../../" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
